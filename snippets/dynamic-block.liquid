{% if block.settings.product %}
    <div class="dynamic-section-block-item">
        <div class="dynamic-section-block-item---media">
    
        </div>
        <div class="dynamic-section-block-item---content">
            {% if block.settings.show_title %}
              <h2>{{ block.settings.product.title }}</h2>
            {% endif %}
        </div>
    </div>
{% endif %}
